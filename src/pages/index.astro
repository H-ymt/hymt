---
export const prerender = true;
import Container from "../components/Container.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { bio, stack } from "../data/home";
---

<BaseLayout>
  <Container type="page">
    <h1 class="sr-only">Yamato Handai Personal Website</h1>
    <div class="intro">
      <p class="intro-heading">
        A frontend engineer focused on building fast, accessible experiences for the modern web.
      </p>
      <p class="intro-body">
        I build websites and web applications using WordPress, React, and modern frontend tools,
        with a strong focus on performance, accessibility, and clean, maintainable code.
      </p>
    </div>

    <section class="section">
      <h2 class="section-heading">Biography</h2>
      <ol class="bio-list">
        {
          bio.map(({ id, date, body }, index) => (
            <li
              class="bio-item"
              data-current={index === bio.length - 1 ? "true" : "false"}
            >
              {date && (
                <time class="bio-date" datetime={`${date.replace(".", "-")}-01`}>
                  {date}
                </time>
              )}
              <span class="bio-body">{body}</span>
            </li>
          ))
        }
      </ol>
    </section>

    <section class="section">
      <h2 class="section-heading">Dotfiles</h2>
      <p class="dotfiles-body">
        My personal development environment configuration, including shell, editor, and CLI
        tooling setup.
      </p>
      <a
        class="dotfiles-link"
        href="https://github.com/H-ymt/dotfiles"
        target="_blank"
        rel="noopener noreferrer"
      >
        github.com/H-ymt/dotfiles
      </a>
    </section>

    <section class="section">
      <h2 class="section-heading">What I can do</h2>
      <div class="stack-container">
        {
          stack.map((s) => (
            <div class="stack-card">
              <h3 class="stack-category">{s.category}</h3>
              <div class="stack-items">
                {s.items.map((item) => (
                  <div class="stack-item">
                    {item.logo && (
                      <img
                        class="stack-logo"
                        src={item.logo}
                        alt=""
                        width="30"
                        height="30"
                      />
                    )}
                    <p class="stack-label">{item.label}</p>
                  </div>
                ))}
              </div>
            </div>
          ))
        }
      </div>
    </section>
  </Container>
</BaseLayout>

<style>
  .intro {
    display: grid;
    row-gap: 1rem;
    padding-block: 3rem 2rem;
    text-align: center;
    text-wrap: balance;
  }

  .intro-heading {
    font-size: 2rem;
    line-height: 1.3;
    font-weight: 700;
    color: var(--foreground);
  }

  .intro-body {
    font-size: 1em;
    color: var(--foreground-muted);
  }

  @media screen and (width <= 48rem) {
    .intro {
      padding-block: 0;
    }

    .intro-heading {
      font-size: 1.5rem;
    }
  }

  .section {
    margin-block-start: 8rem;
  }

  .section-heading {
    font-size: 1.125rem;
  }

  .bio-list {
    list-style: none;
  }

  .bio-item {
    display: grid;
    gap: 0.125rem;
    padding-block: 0.5em;
    padding-inline-start: 1.5em;
    font-size: 0.9375rem;
    color: var(--foreground-muted);
    border-inline-start: 0.125rem solid var(--border);
  }

  .bio-date {
    margin-block-end: 0.125rem;
    font-size: 0.95em;
  }

  .bio-item[data-current="true"] {
    font-weight: 500;
    color: var(--foreground);
    border-inline-start-color: var(--foreground);
  }

  .dotfiles-body {
    margin-block: 0.875rem 0.75rem;
    font-size: 0.9375rem;
    color: var(--foreground-muted);
  }

  .dotfiles-link {
    font-size: 0.9375rem;
    color: var(--foreground);
    text-decoration: underline;
    text-underline-offset: 0.25em;
  }

  .stack-container {
    display: grid;
    gap: 3rem;
  }

  .stack-card {
    width: 100%;
  }

  .stack-category {
    margin-block: 0.75rem 0.875em;
    font-size: 1rem;
    font-weight: 500;
  }

  .stack-items {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(5.5rem, 1fr));
    gap: 2rem;
  }

  .stack-item {
    display: flex;
    gap: 0.5rem;
    align-items: center;
    font-size: 0.875rem;
    border-radius: 0.375rem;
  }

  .stack-label {
    font-size: 0.75rem;
    line-height: 1.5;
    color: var(--foreground);
  }
</style>
